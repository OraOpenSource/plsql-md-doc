/**
 * A collection type variable of the test_type which contains a varchar2 and a number
 *
 * The Types/Constants/Variables and Exceptions may be removed from the HTML/MD page because it is not available in this context
 */
create or replace type test_types force as table of test_type;
